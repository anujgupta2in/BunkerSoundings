[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA-style fallback (safe even if you don't use React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
